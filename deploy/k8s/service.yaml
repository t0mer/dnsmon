apiVersion: v1
kind: Service
metadata:
  name: dnsmon
  labels:
    app: dnsmon
spec:
  type: ClusterIP
  selector:
    app: dnsmon
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
