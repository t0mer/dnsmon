# Compiled binary
bin/

# Node modules (Tailwind build-time only)
web/node_modules/

# SQLite database
*.db
*.db-shm
*.db-wal

# Air hot-reload temp dir
tmp/

# OS
.DS_Store

# Editor
.idea/
.vscode/

# goreleaser dist (repo root only)
/dist/

# Generated web build output, except the embedded third-party vendor assets
# (leaflet, alpine, flag-icons) which are not produced by `make ui`.
web/dist/*
!web/dist/vendor/

# Project/agent instructions — kept local, not tracked
CLAUDE.md
